/*
 * Validates a missing target with supported unqualified reference syntax.
 * Binding must report a missing reference, not unsupported syntax or success.
 */

/** {@inheritDoc missing} @public */
export declare function derived(value: string): string;
