/*
 * An unused selected dependency must still have a compatible documentation model.
 */

// Keep the module overview separate from the first tested documentation block.
export {};

/** Consumer operation. @public */
// No dependency reference is needed for selected missing or incompatible models to fail processing.
export declare function consumer(): void;
