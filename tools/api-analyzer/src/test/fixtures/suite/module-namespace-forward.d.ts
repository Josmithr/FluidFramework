/* Consumes namespace metadata and selector links through selected dependency models. */
/** Ignored {@link Missing}. @beta */
export { Tools as Forwarded } from "consumer";
/** See {@link consumer#(Tools:namespace).Contract}. @beta */
export declare function use(): void;
