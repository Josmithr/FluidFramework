/* Supplies source metadata and mixed overloads for re-export conflict tests. */
/** Source documentation. @beta */
export declare function foo(): void;
/** Text overload. @beta */
export declare function local(value: string): string;
/** Number overload. @public */
export declare function local(value: number): number;
