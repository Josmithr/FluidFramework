/*
 * Supplies the dependency declaration selected by the default package export condition.
 * Its literal property type must remain distinct from the browser result in the same session.
 */

export interface Environment {
	value: "node";
}
