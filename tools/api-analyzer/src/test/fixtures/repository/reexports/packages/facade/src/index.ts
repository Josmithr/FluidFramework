/* Re-exports retain origin identities across ordinary, renamed, star, and type-only paths. */
export * from "@scenario/core";
export { source as renamedSource } from "@scenario/core";
export type { Contract as ContractType, source as SourceType } from "@scenario/core";
