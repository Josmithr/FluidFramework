## API Report File for "example"

> Generated by api-analyzer. Do not edit directly.

Surface: `"beta"`

```ts
// @public
export interface Base<Value> {
    callback: (value: Value) => Value;
    conditional: Value extends string ? never : Value;
    dictionary: { [key: string]: Value; };
    get(): Value | undefined;
    result: { nested: readonly Value[]; };
    tuple: [Value, ...Value[]];
}

// @beta
export interface Derived extends Base<Preview> {
    // Inherited from `Base`
    callback: (value: Preview) => Preview;
    // Inherited from `Base`
    conditional: Preview;
    // Inherited from `Base`
    dictionary: { [key: string]: Preview; };
    // Inherited from `Base`
    get(): Preview | undefined;
    // Inherited from `Base`
    result: { nested: readonly Preview[]; };
    // Inherited from `Base`
    tuple: [Preview, ...Preview[]];
}

// @beta
export interface Preview {
    // (undocumented)
    value: string;
}
```
