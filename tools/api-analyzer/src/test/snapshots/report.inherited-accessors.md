## API Report File for "consumer"

> Generated by api-analyzer. Do not edit directly.

Surface: `"public"`

```ts
import { Base } from "dependency";
import { Concrete } from "dependency";

// @public
export class ConcreteLeaf extends Concrete {
    // Inherited from `Concrete` in package `dependency`
    get value(): number;
    // Inherited from `Concrete` in package `dependency`
    set value(input: string | number);
}

// @public
export class Leaf extends Middle<string> {
    // Inherited from `Base` in package `dependency`
    protected convert(input: string): string;
    // Inherited from `Base` in package `dependency`
    get only(): string;
    // Inherited from `Base` in package `dependency`
    protected get secret(): string;
    // Inherited from `Base` in package `dependency`
    set sink(input: string);
    // Inherited from `Base` in package `dependency`
    protected state: string;
    // Inherited from `Base` in package `dependency`
    get value(): string;
    // Inherited from `Base` in package `dependency`
    set value(input: string);
}

// @public
export class Middle<Value> extends Base<Value> {
    // Inherited from `Base` in package `dependency`
    protected convert(input: Value): Value;
    // Inherited from `Base` in package `dependency`
    get only(): Value;
    // Inherited from `Base` in package `dependency`
    protected get secret(): Value;
    // Inherited from `Base` in package `dependency`
    set sink(input: Value);
    // Inherited from `Base` in package `dependency`
    protected state: Value;
    // Inherited from `Base` in package `dependency`
    get value(): Value;
    // Inherited from `Base` in package `dependency`
    set value(input: Value);
}

// @public
export class Override extends Base<string> {
    get value(): string;
    set value(input: string);
    convert(input: string): string;
    // Inherited from `Base` in package `dependency`
    get only(): string;
    // Inherited from `Base` in package `dependency`
    protected get secret(): string;
    // Inherited from `Base` in package `dependency`
    set sink(input: string);
    // Inherited from `Base` in package `dependency`
    protected state: string;
}
```
