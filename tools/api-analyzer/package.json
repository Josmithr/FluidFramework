{
	"name": "api-analyzer",
	"version": "0.0.0",
	"private": true,
	"description": "Experimental reusable API analysis with native TypeScript 7.",
	"license": "MIT",
	"type": "module",
	"exports": {
		".": {
			"types": "./lib/index.d.ts",
			"import": "./lib/index.js"
		}
	},
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"check:format": "biome check .",
		"format": "biome check . --write",
		"test": "npm run build && npm run test:mocha",
		"test:contracts": "npm run build && mocha \"lib/test/**/*.test.js\" --grep \"Effective configuration|Analysis session|Adapter fact extraction|Release classification and metadata selection|session.*worker\" --timeout 20000",
		"test:stage1": "npm run build && mocha \"lib/test/**/*.test.js\" --grep \"Effective configuration|Analysis session|Adapter fact extraction|session.*worker\" --timeout 20000",
		"test:mocha": "mocha \"lib/test/**/*.test.js\" --timeout 20000"
	},
	"devDependencies": {
		"@biomejs/biome": "2.4.5",
		"@types/mocha": "10.0.10",
		"@types/node": "22.19.17",
		"mocha": "11.7.5",
		"typescript": "7.0.2",
		"typescript6": "npm:typescript@6.0.3"
	},
	"engines": {
		"node": ">=22.22.2"
	},
	"packageManager": "pnpm@11.15.1",
	"dependencies": {
		"@eslint/object-schema": "3.0.5",
		"@microsoft/tsdoc": "0.16.0"
	}
}
